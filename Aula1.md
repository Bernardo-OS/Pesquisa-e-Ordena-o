Conceitos Iniciais:


CRUD (Create, Retrieve, Update, Delete)

Search = FInd = In = Contains

Complexidade = Esforço (memória, processamento, recusro, tempo) = Big O -> O(n!), O(n^x), O(n)...
**ORDENAR** ||| **ÁRVORE**
- Qtd Comparações  (Ordenação) (Pesquisa)
- Qtd Trocas       (Ordenação)

maior Estabilidade NÃO significa Complexidade menor
